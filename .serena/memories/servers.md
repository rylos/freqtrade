# Server (RyLoS Classic, aggiornato 2026-07-21 sera)

## debian — hyperopt/backtest freqtrade
- SSH: `ssh marco@192.168.0.34` (28 core)
- Path: `/opt/freqtrade`, venv `.venv` (Python 3.11)
- **Sync via git**: fork `github.com/rylos/freqtrade`, branch develop → `git pull origin develop`
- tmux `ft-hyperopt`: hyperopt RyLoS (~22 epoch/min con -j 30). NON toccare i tmux passivbot: `back`, `back2`, `opt`
- **2026-08-07: run seedato CHIUSO a 7.940 epoch** (fermato da Marco: aveva già dato la sua risposta). Miglior risultato epoch 7940, objective −36,30327, archiviato in `user_data/candidates/tmf_chal_ep7940_2026-08-07.json` e **scartato** come 2013 e 5323. ✅ Pulizia fatta: `.fthypt` da **13,8 GB** rimosso, `hyperopt_tickerdata.pkl` rimosso, **nessun json fantasma** prodotto dall'interruzione (verificato), disco da 53% a **42%**. tmux `ft-hyperopt` libero
- **2026-08-07 01:45 (storico)**: run seedato con la dimensione nuova `dca_tmf_weight` (TMF solo-rialzo sullo stake DCA, commit `ecbf0e7e1`). Dati riscaricati fino al 2026-08-06 23:35, range `20241205-20260807`, `--spaces buy sell stoploss`, 10k epoch, -j 30, log in `/tmp/hyperopt_tmf.log`. Seed verificato = T4G + `dca_tmf_weight` 0.6. ⚠️ objective NON confrontabile coi run precedenti (range diverso). A fine analisi **cancellare il .fthypt** (16-20GB) e il json fantasma
- **2026-08-10: `user_data/strategies/RyLoSStrategy.json` = T5** (`2b6d6c5ada49bb9fabfd8d50a3b063e7`, da `candidates/t5_2026-08-10.json`), `.py` `15717b2ba03f378eb93a8ab601de26d3`. **Regressione di riferimento: 817 trade, +22.312,23%, uw 11,97%, Sortino 6,40** (era T4V: 810 trade, +17.008,01%, uw 19,15%)
- **Plot T5 (2026-08-10) in `user_data/plot/`**: `t5-01-ingressi-osc4rsi.html`, `t5-02-tmf.html`, `t5-03-crashguard-ret6.html` (il `ret6_pct` nuovo di T5), `t5-04-stocastico.html`, più `freqtrade-profit-plot.html`. Via sftp da debian, vedi `mem:plot-delivery`
- 🧪 **Metodo A/B usato la notte del 2026-08-09/10**: un braccio per directory `user_data/ab<N>/<nome>/` con **copia isolata** di `.py`+`.json`, lanciato con `--strategy-path`, più un braccio `off` di controllo. Il json attivo e il `.py` non si toccano mai. Directory di lavoro **cancellate a fine sessione**
- **2026-08-07 (storico): `RyLoSStrategy.json` = T4V** (`00cef67abc160a5f2cc3db8481ff1f2c`). Prima NON esisteva e i backtest usavano i default di classe
- **Plot T4V (2026-08-07, storici) in `user_data/plot/`**: `t4v-profit.html` (31 MB), `t4v-dataframe-full-osc.html` (44 MB, `osc_4rsi`+`stoch_k`), `t4v-dataframe-full-tmf.html` (38 MB, `tmf_z`), `t4v-dataframe-drawdown-lug.html` (8 MB, 20/06→07/08, leggibile). Backtest sotto: 810 trade, +17.008,01%, dd 4,66%, uw 19,15%, Sortino 2,64 = regressione di riferimento
  - ⚠️ **`backtesting --export-filename` viene IGNORATO**: freqtrade scrive sempre `backtest-result-<timestamp>.zip`. Prendere il nome da `backtest_results/.last_result.json` e passarlo a `plot-profit`/`plot-dataframe`, altrimenti "Backtest file is missing, skipping trades"
  - ⚠️ La CLI ha **un solo pannello secondario** (`--indicators2`): `tmf_z` (−1..1) e `stoch_k` (0..100) insieme sono illeggibili → due file separati
- Dati: HYPE/USDT:USDT 5m futures dal 2024-12-05 + funding/mark
- Config: `user_data/config.json` (bybit, HYPE, max_open_trades 1, futures isolated)
- Utility in /tmp: `extract_epoch.py N` (estrazione epoch), `loss_audit.py` (audit correlazioni loss)
- Passivbot (NON toccare): `/opt/passivbot`, config dd39 in `backtests/combined/2026-07-20T11_47_30_dd39/config.json`

## AWS — live trading (`ssh admin@amazon.ziliani.net`, 2 core ARM, 4GB)
- **`/opt/freqtrade`**: ricreato da zero 2026-07-21 (il vecchio era cancellato). Clone shallow fork (https, develop), venv `.venv` (Python 3.11.2, install base), ta-lib C di sistema
- **Config** `user_data/config.json` (chmod 600): bybit chiavi `bybit_02` (importate da `/opt/passivbot/api-keys.json`), HYPE/USDT:USDT futures isolated, max_open_trades 1, stake unlimited, pricing orderbook "other", **dry_run: true, dry_run_wallet 8200**. Telegram: **@freqtraderylos_bot** (token 1363889286:..., chat_id 46772914). api_server NON nel config (schema rifiuta campi vuoti: omettere = disabilitato)
- **⭐⭐⭐ STATO AL 2026-08-10 00:34 CEST — LIVE SU T5**: PID **186395**, versione **`2026.8-dev-fbe94ec`**, `.py` md5 **`15717b2ba03f378eb93a8ab601de26d3`**, json T5 md5 **`2b6d6c5ada49bb9fabfd8d50a3b063e7`** (= `candidates/t5_2026-08-10.json`), tag **`rylos-t5-live-20260810`**. Deploy **a bot flat** (0 trade aperti, 5 chiusi, realizzato −668,33), `state=RUNNING`, zero errori dopo il riavvio
  - **Cosa cambia**: `time_exit_days` 4→3, `time_exit_qty_pct` 0,25→0,50, `unstuck_threshold` 0,681→**1,00 (unstuck spento)**, nuovo `crash_guard_pct` **8,0**. Backtest: 817 trade, **+22.312,23%**, uw **11,97%**, Sortino **6,40**, dd conto 5,30%
  - ✅ **Procedura seguita**: `touch ~/watchdog/ft_maintenance` → backup completo in **`~/backup-t4v-live-20260810/`** (py, json, config, db, pip-freeze) → `tmux send-keys -t ft C-c` → `git checkout -- ` + `git pull --depth=50` → `cp candidates/t5_2026-08-10.json strategies/RyLoSStrategy.json` → verifica md5 → `list-strategies` come smoke test → riavvio → verifica log → **`rm ~/watchdog/ft_maintenance`**
  - ✅ **Trappola `.py`/`.json` verificata**: il log mostra `Loading parameters from file .../RyLoSStrategy.json` e poi `time_exit_days = 3`, `time_exit_qty_pct = 0.5`, `unstuck_threshold = 1.0`, `crash_guard_pct = 8.0` → i valori vengono dal json, non dai default di classe. **Controllare SEMPRE queste righe dopo un deploy**
  - ⚠️ `ps -eo pid,cmd | grep "[f]reqtrade trade"` invece di `pgrep -af`: via ssh il comando matcha sé stesso
  - **Rollback**: `git checkout rylos-t4v-live-20260808` + `cp user_data/candidates/t4v_2026-08-07.json user_data/strategies/RyLoSStrategy.json`
- **STATO AL 2026-08-07 09:21 (storico) — T4V**: PID **176967**, versione **`2026.8-dev-f44881c`**, `.py` md5 `74c00f066dae456d9d5ce2540845f8ad`, json T4V md5 `00cef67abc160a5f2cc3db8481ff1f2c`. Deploy a bot flat, zero errori, saldo 7.352,97 invariato. **ccxt aggiornato a 4.5.71** (era 4.5.67). ⚠️ **TA-Lib resta 0.6.8** su entrambe le macchine di proposito: aggiornarlo solo qui disallineerebbe amazon da debian sulla libreria degli indicatori. Backup completo del precedente in **`~/backup-t4g-live-20260807/`** (`.py`, `.json`, `config.json`, `pip-freeze.txt`, copia db) + tag git `rylos-t4g-live-20260807`
- **STATO AL 2026-08-08 00:17**: PID **179108**, versione **`2026.8-dev-ac30695`**, `.py` md5 **`ade7d76aa6b514cf6fbad00420617eda`** (aggiunto il log del fattore TMF a ogni fill DCA, solo live/dry_run — regressione bit-perfetta). Riavvio **col trade #5 aperto**, ripreso dal db senza problemi
  - ⚠️ **pre-commit non era installato nel venv di pc-work**: i commit passavano solo perché l'hook falliva. Installandolo, mypy/ruff/codespell hanno bocciato la strategia con **problemi preesistenti**, e **ruff-format ha riscritto il file della strategia live** (77 righe). Risolto escludendo `^user_data/` da mypy, ruff, ruff-format e codespell in `.pre-commit-config.yaml` (+ `^\.serena/` per codespell): sono codice nostro, non sorgente upstream. Senza l'esclusione ogni commit riformatta la strategia in produzione
- ✅ **MACCHINE ALLINEATE (2026-08-07 10:25)**: debian e amazon entrambe su **ccxt 4.5.71, pandas 3.0.5, numpy 2.4.6, TA-Lib 0.7.1, technical 1.7.0, SQLAlchemy 2.0.51**, `pip check` pulito. Wheel TA-Lib disponibile per x86_64 e aarch64 → nessuna compilazione. Backup dei pip freeze precedenti in `~/backup-venv-20260807/pip-freeze-PRIMA.txt` su entrambe. **Regressione T4V bit-perfetta dopo l'aggiornamento** (810 trade, +17.008,01%). Bot riavviato su amazon a bot flat: PID **177676**, zero errori, saldo 7.352,97 invariato
- ⚠️ Il residuo di 1e-15 nel `tmf_z` fra le due macchine è **architetturale** (x86_64 vs aarch64), non di versione: resta anche con librerie identiche. Irrilevante, non inseguirlo
- **Stato al 2026-08-06 (dopo il merge upstream `fca309cfc`)**: bot riavviato a bot flat su **`2026.8-dev-fca309c`** (PID 172022), **flat dal 04/08 12:02**, zero posizioni su bybit. **Saldo reale 7.333,10 USDT** (da 8.200 al go-live). Verifica saldo/posizioni: script ccxt con le chiavi del config via `.venv/bin/python` (`fetch_balance`, `fetch_positions`, `fetch_ticker`) — il db dà i P&L dei trade ma NON il funding
- **Stato al 2026-08-04**: live (`dry_run: false`) su **`2026.8-dev-a4ed5fa`** (merge upstream del 04/08, deploy fatto col trade 3 aperto: freqtrade lo riprende dal db, `Found open trade` nel log di avvio). `.py` md5 `857bccc947ca8b4f8ffabc6b507be982`, json T4G md5 `23baf272dbd3e40fb17e9945f3bf6c75`. Prima del riavvio conviene un `list-strategies` come smoke test del codice nuovo sul venv esistente
- **Stato al 2026-07-26**: live (`dry_run: false`) su `2026.7-dev-b0eabf4` dopo il merge upstream; `.py` consolidato coi default 5371 + json live invariato. **Riavvio bot**: ⚠️ dal 2026-08-06 PRIMA di fermarlo `touch ~/watchdog/ft_maintenance` (senza, il watchdog lo rimette su entro 10 minuti mentre ci lavori) e `rm ~/watchdog/ft_maintenance` a fine intervento. Poi `tmux send-keys -t ft C-c` (l'`AttributeError: '_abort'` + errore Telegram in chiusura sono normali), attendere ~12s, poi `tmux send-keys -t ft "cd /opt/freqtrade && .venv/bin/freqtrade trade -c user_data/config.json" Enter`. ⚠️ `pgrep -f "freqtrade trade"` via ssh matcha sé stesso: usare `pgrep -af "bin/freqtrade"`. Update codice: `git checkout -- user_data/strategies/RyLoSStrategy.py` (clone shallow, il file è tracciato e modificato) poi `git pull --depth=50 origin develop`, e verificare md5 = `227d4b88a954a368ef4f97309e68b1eb`
- **WATCHDOG del bot freqtrade (dal 2026-08-06)**: `~/watchdog/freqtrade_watchdog.py` (utente admin), cron `5-59/10` (sfasato di 5 min dal watchdog passivbot che gira a `*/10`), log `~/watchdog/freqtrade_watchdog.log` (solo anomalie) + `ft_state.json`. Il bot non scrive su file → legge il **buffer del tmux `ft`** e il db. Controlla: processo `bin/freqtrade` vivo · heartbeat <10 min (normale ~60s) · `state=RUNNING` · righe ERROR/CRITICAL negli ultimi 15 min · trade aperti dal db in read-only
  - ✅ **2026-08-10: il nome del candidato nell'intestazione non è più hardcoded.** Era `"freqtrade RyLoS T4G (amazon)"` ed era sopravvissuto a due cambi di candidato. Ora la funzione `candidato_attivo()` confronta l'**md5 del json attivo** con quelli in `user_data/candidates/` e usa il nome del file che combacia (fallback: md5 breve se nessuno combacia, es. params modificati a mano). **Si aggiorna da solo a ogni deploy**, verificato: restituisce `T5`. Backup del precedente in `~/watchdog/freqtrade_watchdog.py.bak-20260810`
  - ✅ **2026-08-10: versionato in `ops/watchdog_freqtrade.py`** (commit `451443986`) — prima viveva solo su amazon e una ricostruzione della macchina l'avrebbe perso. Gira comunque **dal server**, la copia nel repo è il riferimento: dopo ogni modifica `scp ops/watchdog_freqtrade.py amazon.ziliani.net:'~/watchdog/freqtrade_watchdog.py'` e **verificare che l'md5 combaci** (ora `e1b3482e9687e86b891d5c54c8edb18f`). ⚠️ Entrando nel repo il file passa sotto i pre-commit di upstream: ruff l'ha riformattato e ha preteso `check=False`, `Path.open`, `usedforsecurity=False` su md5; `codespell` esclude `ops/` come già `.serena/` e `user_data/` perché i commenti sono in italiano. Il gemello passivbot è in `passivbot/ops/watchdog_ry_hl.py`, stessa procedura
  - 🪤 **Era già versionato dal 2026-08-06 in `user_data/scripts/freqtrade_watchdog.py` e non me n'ero accorto**: `user_data/` è gitignored, quindi non compariva né in `git status` né nei `ls` normali, e la copia è rimasta ferma alla versione con `T4G` hardcoded mentre quella sul server cambiava. Rimosso il duplicato (`a133c47ca`). **Lezione: versionare dentro una directory ignorata non protegge nulla** — il file diventa invisibile e si stantia in silenzio. Per cercare file tracciati lì dentro serve `git ls-files user_data/`, non `ls` né `git status`
  - **Esito su healthchecks.io**, check **"freqtrade (bybit)"** `d075b180…` (riusato: era in pausa dal 09/10/2025), `timeout 600` / `grace 900`: ping di successo col riepilogo nel body, `/fail` con la diagnosi in caso di anomalia; se cron o macchina muoiono il check scade da solo = dead man switch. ⚠️ **L'URL di ping è una credenziale** (chi ce l'ha può segnare "up" un bot morto): sta solo in `~/watchdog/healthchecks.env` (chmod 600), MAI nelle note
  - **RIAVVIO AUTOMATICO solo a processo ASSENTE** (scelta di Marco 2026-08-06). Razionale: `stoploss_on_exchange` è disattivo, quindi il guard-stop −0.721 vive nel bot e a bot morto una posizione aperta resta senza protezione. Un bot **vivo ma bloccato NON viene toccato**: resta in allarme e decide Marco. Se manca la sessione tmux `ft` la ricrea. Freni: max 2 tentativi/ora poi si arrende, e **file `~/watchdog/ft_maintenance` = nessun riavvio** (da creare per le fermate volute: deploy, cambio candidato)
  - ⚠️ **Gli errori del canale Telegram non sono guasti**: la prima versione ha dato subito un falso positivo su `telegram.ext.Updater - ERROR - Exception happened while polling for updates` (rumore di rete). Ora tutto ciò che matcha `telegram` viene contato nel body ma non fa scattare l'allarme
  - Percorsi di riavvio provati su sessione finta `ft-wdtest` con un `sleep` al posto di freqtrade, senza toccare il bot vero: guardia di manutenzione, creazione sessione + rilancio, freno rate limit
- **tmux `ft`**: bot RyLoS dry-run col candidato hyperopt corrente (`user_data/strategies/RyLoSStrategy.json` = params epoch deployato). Aggiornamento candidato: scp nuovo json params (CON chiave "stoploss"!) + **rm tradesv3.dryrun.sqlite*** + riavvio
- Dal 2026-07-22: **epoch 5371** in dry-run (stoploss -0.721 confermato nel log di avvio; con leva 4x lo stop sta ~3-4 punti di prezzo sopra la liquidazione → la liquidazione non viene mai toccata)
- Altri tmux da NON toccare: `ry`/`ry-hl` (passivbot live), `ft-hl` (vecchio), `mm`
- Go-live: dry-run ≥24h col candidato finale → `dry_run: false` su ok esplicito di Marco
- **Piano passaggio di consegne (2026-07-22, GESTISCE MARCO in autonomia)**: passivbot `ry` opera su bybit_02 (HYPE, hedge mode, posizione aperta) → chiusura posizione → stop tmux `ry` → switch account a one-way mode (freqtrade non supporta hedge) → poi go-live freqtrade. Freqtrade NON può adottare posizioni non sue; due bot sulla stessa coin/account non possono coesistere
- **Esecuzione ordini**: limit "aggressivi" al 2° livello del lato opposto dell'orderbook (price_side "other", order_book_top 2) per entry/exit/clip
  - ⚠️ **CORREZIONE 2026-08-08**: qui c'era scritto "stoploss = market", ed era **FALSO**. Il default di `IStrategy.order_types` è `stoploss: limit` e il log di avvio lo confermava: **anche il guard-stop era un ordine limit**, quindi in un crollo veloce poteva restare appeso (liquidazione a ~41 contro stop a ~44,6). Non fidarsi dei default a memoria: leggere la riga `Strategy using order_types` nel log di avvio
  - ✅ **Corretto il 2026-08-08 20:08** (bot flat, PID **181839**), via `config.json` per non toccare l'md5 della strategia: `order_types` = entry limit, exit limit, **stoploss market**, **emergency_exit market**; `unfilledtimeout` = entry 10, **exit 2**, **exit_timeout_count 3**, unit minutes. Backup del config precedente in `~/backup-config-20260808.json`
  - **Perché**: misurato sul book, i primi 2 livelli bid coprono ~37 HYPE contro una posizione di 260 → **14%**. L'uscita del trade #5 ha impiegato **4 min 10 s** per riempirsi (piazzata 16:53:11, chiusa 16:57:21), pur senza slippage (tutto a 54,978, il limite richiesto). Storico: 26 ordini di uscita, **tutti riempiti al 100%**, mai un residuo; le clip piccole (15-25 HYPE) partono nello stesso secondo. `exit_timeout_count 3` è la valvola: dopo 3 tentativi falliti scatta `emergency_exit`, che è **market** di default → garanzia di chiusura senza slippage sulle uscite normali
  - ⏸️ **Rimandato di proposito**: alzare `order_book_top` delle uscite da 2 a 4-5. Peggiorerebbe il prezzo su OGNI uscita (comprese le decine di clip perfette) per un rischio che finora non è mai costato nulla. Prima misurare i tempi di riempimento sui prossimi trade
  - ⚠️ I timeout **non** aspettano la chiusura delle candele: `manage_open_orders` gira nel ciclo del bot ogni pochi secondi (`freqtradebot.py:1602`). Le candele contano solo per i segnali
- **Decisione Marco 2026-07-22: `stoploss_on_exchange` resta DISATTIVO per ora** (proposto per proteggere il guard-stop da crash del VPS, rifiutato consapevolmente — non riproporlo, ma è il primo candidato se in live si verificano disservizi del bot)
- Il soak test dry-run ha già scovato un bug live invisibile al backtest (unità timestamp) — vedi `mem:tech`

## pc-work / pc-casa
- pc-work: sviluppo, `/home/marco/dev/freqtrade`
- pc-casa: `ssh -p 22222 marco@home.ziliani.net`; screenshot in `/home/marco/Immagini/Schermate/` (recuperare con scp quando li cita)
